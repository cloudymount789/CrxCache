# CrxCache
CrxCache is a high-performance C++11/14 in-memory cache. It supports LRU, LFU, and ARC strategies. By using cache sharding to reduce lock contention, it ensures high throughput and low latency in concurrent environments. Perfect for performance-critical middleware and backend services.
